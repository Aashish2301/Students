@model IEnumerable<WebApplication2.Models.webpages_Roles>

@{
    ViewBag.Title = "Index";
}

<h2 >Roles</h2>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoleName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoleCode)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoleName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoleCode)
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = item.RoleId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.RoleId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.RoleId })*@


                <a href="@Url.Action("Edit",new { id = item.RoleId })" class="btn btn-primary"> Edit</a>
                <a href="@Url.Action("Details",new { id = item.RoleId })" class="btn btn-success">Details</a>
                @*<a href="@Url.Action("Delete",new { id = item.RoleId })" class="btn btn-danger">Delete</a>*@
            </td>
        </tr>
    }

</table>

